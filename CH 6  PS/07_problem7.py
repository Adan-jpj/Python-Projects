# Write a program to find out whether a given post is talking about "Harry or not.hasattr"

post = input("Enter the post:")

if ("Adan".lower() in post.lower()):
    print("This post is talking about Adan")

else:
    print("This post is not talking about Adan")